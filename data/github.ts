/** GitHub API configuration for portfolio and goals analytics */
export const githubConfig = {
  username: "mahdijnt",
  /** Optional subset of repos; empty = all public non-fork repos */
  trackedRepos: [] as string[],
} as const;
